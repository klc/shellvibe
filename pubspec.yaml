name: terly2
description: "Terly2 — Cross-platform (iOS, Android, macOS, Windows, Linux) terminal, SSH/SFTP client and remote infrastructure workstation built with Flutter, dart_ssh2, xterm2 and Zero-Knowledge E2EE encryption."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.9

  # State management
  # Flutter's current flutter_test pins require the analyzer <13 generation line.
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.3

  # Navigation & Routing
  go_router: ^17.3.0

  # Terminal & SSH Engine
  # Kept in-tree until the Android IME action/composition ordering fix is
  # released upstream. See third_party/xterm2/README.md.
  xterm2:
    git:
      url: https://github.com/klc/xterm2.git
  dartssh2: ^2.22.5
  flutter_pty: ^0.4.2

  # Database & Storage
  drift: ^2.34.3
  flutter_secure_storage: ^10.3.1
  cryptography: ^2.9.0
  local_auth: ^3.0.2

  # Desktop & Window Management
  window_manager: ^0.5.2
  tray_manager: ^0.5.3
  hotkey_manager: ^0.2.3
  desktop_drop: ^0.7.1

  flutter_localizations:
    sdk: flutter

  # Utilities
  # Pinned to 0.20.2 by flutter_localizations in the current Flutter SDK.
  intl: ^0.20.2
  url_launcher: ^6.3.2
  path_provider: ^2.1.6
  path: ^1.9.1
  uuid: ^4.6.0
  shadcn_ui: ^0.56.0
  lucide_icons_flutter: ^3.1.15
  google_fonts: ^6.2.1
  file_selector: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  sqlite3: ^3.5.0
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5

  # Code generation
  # 2.16.x requires meta ^1.18.3, while flutter_test pins meta 1.18.0.
  build_runner: ^2.15.0
  riverpod_generator: ^4.0.4
  drift_dev: ^2.34.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Terminal Nerd Fonts (Mono variants — every glyph single-cell wide, which
  # keeps xterm2's grid alignment because xterm2 treats the Private Use Area
  # as width 1). Licenses in assets/fonts/nerd/LICENSES.txt.
  fonts:
    - family: JetBrainsMono Nerd Font Mono
      fonts:
        - asset: assets/fonts/nerd/JetBrainsMonoNerdFontMono-Regular.ttf
    - family: FiraCode Nerd Font Mono
      fonts:
        - asset: assets/fonts/nerd/FiraCodeNerdFontMono-Regular.ttf
    - family: MesloLGM Nerd Font Mono
      fonts:
        - asset: assets/fonts/nerd/MesloLGMNerdFontMono-Regular.ttf
    - family: Hack Nerd Font Mono
      fonts:
        - asset: assets/fonts/nerd/HackNerdFontMono-Regular.ttf
    - family: CaskaydiaCove Nerd Font Mono
      fonts:
        - asset: assets/fonts/nerd/CaskaydiaCoveNerdFontMono-Regular.ttf
    - family: UbuntuMono Nerd Font Mono
      fonts:
        - asset: assets/fonts/nerd/UbuntuMonoNerdFontMono-Regular.ttf
    - family: SauceCodePro Nerd Font Mono
      fonts:
        - asset: assets/fonts/nerd/SauceCodeProNerdFontMono-Regular.ttf
    - family: Symbols Nerd Font Mono
      fonts:
        - asset: assets/fonts/nerd/SymbolsNerdFontMono-Regular.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
